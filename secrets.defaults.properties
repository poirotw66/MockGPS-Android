# Safe fallback used when secrets.properties is absent (for example, CI configuration checks).
# This value cannot load Google Maps. Replace it only in the ignored secrets.properties file.
MAPS_API_KEY=DEFAULT_API_KEY
