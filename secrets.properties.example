# Copy this file to secrets.properties, then replace the placeholder locally.
# secrets.properties is ignored by Git and must never be committed.
# Restrict the real key in Google Cloud to com.sora.mockgps, its SHA-1 signing
# certificate(s), and the Maps SDK for Android API.
MAPS_API_KEY=YOUR_ANDROID_RESTRICTED_MAPS_API_KEY
